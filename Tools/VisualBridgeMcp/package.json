{
  "name": "@visualbridge/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build --workspace @visualbridge/node-host && esbuild src/server.ts src/projectTransaction.ts --bundle --platform=node --format=esm --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\" --outdir=dist --sourcemap && npm run check",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/projectTransaction.test.mjs test/stdio.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "@visualbridge/core": "*",
    "@visualbridge/entity": "*",
    "@visualbridge/graph": "*",
    "@visualbridge/node-host": "*",
    "@visualbridge/structured": "*",
    "@visualbridge/table": "*",
    "minimatch": "^10.2.6",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^24.13.1",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  }
}
