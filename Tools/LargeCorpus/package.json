{
  "name": "@visualbridge/large-corpus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run check",
    "check": "node --check src/corpus.mjs && node --check src/cli.mjs && node --check src/benchmark.mjs && node --check test/largeCorpus.test.mjs",
    "test": "npm run build --workspace @visualbridge/core && npm run build --workspace @visualbridge/entity && npm run build --workspace @visualbridge/graph && npm run build --workspace @visualbridge/structured && npm run build --workspace @visualbridge/table && node --test test/largeCorpus.test.mjs",
    "generate": "node src/cli.mjs",
    "benchmark": "node src/benchmark.mjs"
  }
}
